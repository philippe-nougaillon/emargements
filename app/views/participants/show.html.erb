<p class="mb-4" style="color: green"><%= notice %></p>

<%= render @participant %>

<div>
  <%= link_to "Modifier", edit_participant_path(@participant), class:'btn btn-success btn-sm' %> |
  <%= link_to "Retour", participants_path, class:'btn btn-sm' %>

  <%= button_to "Supprimer", @participant, method: :delete, class:'btn btn-error btn-sm' %>
</div>
