<div>
  <h1 class="text-4xl font-bold mb-8">Émargements</h1>
</div>

<%= form_tag request.path, method: :get do %>
  <div class="flex items-end gap-4">
    <div class="w-1/6">
      <%= label_tag :search, "Nom/Prénom/Email" %>
      <%= text_field_tag :search, params[:search], include_blank:true, onchange:'this.form.submit()', class: 'input input-bordered input-sm w-full' %>
    </div>
    <div>
      <% tag_cloud(@tags, %w(badge-sm badge badge-md badge-lg)) do |tag, css_class| %>
          <%= link_to url_for(params.permit!.merge(tag: tag.name)), data: { turbo_frame: "results_frame" }, class: "inline #{tag.name == params[:tag] ? "" : "" }" do %>
              <span class="inline-block align-bottom badge <%= css_class %> <%= tag.name == params[:tag] ? "badge-warning" : "badge-outline" %>">
                  <span class=""><%= tag.name.humanize %></span>
                  <% if params[:tag] == tag.name %>
                      <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" class="inline-block w-4 h-4 stroke-current"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path></svg>
                  <% end %>
              </span>
          <% end %>
      <% end %>
    </div>
  </div>
<% end %>

<div id="presences" class="flex flex-wrap gap-4 justify-between px-12 mb-12">
  <% @presences.each do |presence| %>
    <%= render presence %>
  <% end %>
</div>

<p class="mt-8">Affichage de <b><%= @presences.count %></b> émargements sur <%= Presence.all.count %> au total</p>

<%= link_to presences_path(format: :xls), class: "text-success hover:underline mb-12" do %>
  Exporter vers Excel
<% end %>